spring:
  application:
    name: letterboxd

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  server:
    port: ${PORT:8080}

  sql:
    init:
      mode: always

  thymeleaf:
    cache: false